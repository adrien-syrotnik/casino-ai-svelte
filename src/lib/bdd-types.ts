export type PlayerData = {
    name: string,
    id: string,
    balance: number,
    created_at: string
}

export type BestWin = {
    multiplier: number,
    date: string,
    username: string,
    bet: number
    reward: number
}